## Migrations

Sometimes the internal formats get updated. In order to keep
the project going, some files need updating.

## Steps

1. Rename all relevant files, adding a `_` at the start of their names
2. Invoke the relevant migration script
